---
doc_id: SYNC-PRD-001
type: PRD
title: PRD — 싱크독
status: draft
upstream: [SYNC-RFQ-001]
---

# PRD: 싱크독 (SyncDoc)

---

## 1. 목표

#### G1 평소 쓰는 에이전트와 대화하다가 결정된 내용이 그대로 명세로 남는다

#### G2 11단계 명세 체인을 한 곳에서 조회하고 확정한다

#### G3 명세끼리 항목 단위로 연결되어 근거를 역추적할 수 있다

#### G4 상위 명세가 바뀌면 영향받는 하위 명세가 자동으로 식별된다

#### G5 각자 쓰는 에이전트가 툴에 상관없이 명세를 읽고 쓸 수 있다

#### G6 명세 원본은 에이전트가 읽기 좋게 쓰고, 사람은 거기서 파생된 뷰로 본다

#### G7 팀원 2~3명이 같은 명세를 공유하고 서로 검토한다


## 2. 비목표

| 비목표 | 이유 |
|---|---|
| 플랫폼 내 AI 기능 (초안 생성·정합성 검사) | 각자의 에이전트가 MCP로 붙어서 한다. 플랫폼은 보관과 전달까지 |
| 웹 원본 편집기 | 명세 본문은 각자의 에이전트가 MCP로 쓴다. 사람이 웹에서 직접 고치면 에이전트가 관리하는 흐름과 어긋나고, 검사·충돌·삭제 확인을 사람이 손으로 하게 된다 |
| 사람용 뷰 직접 편집 | MD 원본이 유일한 원본. 뷰 편집을 허용하면 원본이 둘이 되어 반드시 어긋남 |
| 코드 저장소 대체 | 코드는 GitHub에 두고 링크만 연결 |
| 단계 순서 강제 | RFQ에서 "권장이지 강제 아님"으로 확정 |
| 역할·권한 관리 | 2~3명이 서로 대화하며 쓴다. 누구나 쓰고 누구나 승인한다 |
| 이슈 트래커·일정 관리 | 별도 도구 영역 |
| 기존 문서 이관 | 앞으로의 문서만 싱크독에서 쓴다 |
| 역할별 권한 인프라 | 접근은 GitHub OAuth로 걸되, 들어온 사람은 모두 같은 일을 한다 |

---

## 3. 요구사항

10개 기능 전부 v1 범위다. 우선순위 구분은 두지 않되, R1·R5는 나머지가 올라서는 바닥이므로 먼저 구현한다.

### 3.1 기능 요구사항

#### R1 에이전트용 원본과 사람용 뷰

근거: [[SYNC-RFQ-001#Q1]]

명세는 에이전트가 읽을 것을 전제로, 규약이 있는 Markdown으로 작성한다. 이 MD가 원본이자 에이전트용 산출물이다. 항목 ID·`[[]]` 참조 문법·다이어그램 코드블록·frontmatter는 모두 에이전트가 파싱하기 위한 규약이다.

사람용 뷰는 원본에 그림·참조 링크·상태 표시를 더해 파생 생성한다. 마크다운을 HTML로 변환하는 것이 아니라, 사람이 보는 데 필요한 정보를 얹은 별도 표현이다.

- [ ] 원본 규약은 [[SYNC-STD-001]] 명세 작성 규약을 따른다. **규약 위반**(파싱 불가)은 저장을 거부하고, **미완성**(필수 절 누락)은 저장하되 경고한다
- [ ] 에이전트가 원본 MD를 그대로 읽어도 문서 구조·참조·상태를 파악할 수 있다
- [ ] 사람용 뷰에서 다이어그램은 그림으로, `[[]]`는 클릭 링크로, 상태·플래그는 뱃지로 표시된다
- [ ] 웹 문서 뷰는 **유저용**(사람용 뷰, 기본)과 **원본**(에이전트가 읽는 MD 그대로) 두 탭을 전환해 보여준다. 원본 탭은 읽기와 복사만 된다
- [ ] 사람용 뷰에 목차와 단계 간 이동이 있다
- [ ] 사람용 뷰 자체는 편집 대상이 아니며, 원본이 바뀌면 갱신된다
- [ ] 11단계 문서 타입별 원본 템플릿을 싱크독이 관리한다. 프로젝트 초기화 시 저장소 `docs/specs/_templates/`에 복사되며, 이후 그 프로젝트는 저장소의 사본을 쓴다
- [ ] 템플릿은 MCP 도구의 문서 생성에 적용된다

#### R2 ID 기반 상호참조

근거: [[SYNC-RFQ-001#Q3]]

문서와 문서 내 항목에 ID를 부여한다. 본문에서 `[[ID]]`로 참조하면 저장 시 참조 관계가 추출되어 기록된다. 항목 클릭 시 상위 참조(근거)와 하위 참조(파생)를 모두 보여준다. ID 규칙은 5장.

- [ ] 저장 시 본문의 참조가 자동 추출되어 참조 테이블에 반영된다
- [ ] 존재하지 않는 ID를 참조하면 저장은 되되 해당 줄에 경고가 표시된다
- [ ] 항목 상세에서 상·하위 참조가 문서 타입별로 구분되어 보인다
- [ ] 항목 ID는 **작성자(사람 또는 에이전트)가 본문에 직접 붙인다.** 시스템은 발급하지 않고 형식·문서 내 유일성·삭제된 ID 재사용 여부만 검증한다
- [ ] 항목 ID는 발급 후 재사용되지 않는다 (삭제해도 회수 안 함)
- [ ] `[[` 입력 시 같은 프로젝트의 ID가 자동완성된다
- [ ] 프로젝트 전체의 참조 그래프를 조회할 수 있다

#### R3 버전 관리

근거: [[SYNC-RFQ-001#Q3]]

저장할 때마다 새 버전이 생긴다. 덮어쓰기 없음. 버전 = GitHub 커밋. 두 버전 간 diff를 줄 단위로 보여준다.

- [ ] 저장 10번이면 버전 10개가 조회된다
- [ ] 버전 두 개를 골라 diff를 볼 수 있다
- [ ] 버전마다 작성 주체(사람 / 에이전트)와 계정이 기록된다
- [ ] 되돌리기는 "이전 내용으로 새 버전 생성"이며 이력이 지워지지 않는다

#### R4 변경 추적

근거: [[SYNC-RFQ-001#Q3]]

항목이 수정되면 그 항목을 참조하는 하위 항목에 `확인 필요` 플래그가 붙는다. 담당자가 확인 처리하면 해제되고 이력이 남는다. 상위 항목이 삭제되면 `끊어진 참조` 플래그가 붙는다. **반대 방향도 있다** — 하위 문서가 상위와 어긋나게 됐을 때 상위 항목에 `하위 불일치` 플래그가 붙는다. 참조는 하위→상위로만 적히므로 이 방향은 자동 감지가 안 되고, 사람(승인 시 대조)이나 에이전트(저장 시 지정)가 표시한다.

- [ ] 상위 항목 수정 시 이를 참조하는 하위 항목 전부에 플래그가 표시된다
- [ ] 플래그는 문서 목록·대시보드·항목 상세 세 곳에서 보인다
- [ ] "확인함" 처리 시 누가·언제·수정 여부가 기록된다
- [ ] 작성자가 "하위 전파 안 함"을 고를 수 있고, 선택과 사유가 이력에 남는다
- [ ] 항목 삭제 시 하위 참조 목록을 보여주고 확인받은 뒤 `끊어진 참조` 플래그를 붙인다
- [ ] 하위 문서를 `승인`할 때 그 문서가 참조하는 상위 항목 목록을 보여주고, 사람이 어긋난 것을 표시하면 그 상위 항목에 `하위 불일치` 플래그가 붙는다. 건너뛸 수 없다
- [ ] 에이전트가 저장할 때 어긋난 상위 항목을 지정하면 같은 플래그가 붙는다
- [ ] `하위 불일치` 플래그는 상위 문서 담당자의 내 할 일에 뜨고, 확인 처리 화면에서 지목한 하위 항목 본문을 보여준다

#### R5 스토리지 (GitHub 연동)

근거: [[SYNC-RFQ-001#Q3]]

명세 원본(MD)은 **각 프로젝트의 코드 저장소 안 `docs/specs/`** 에 둔다. 명세와 코드가 같은 저장소에서 같이 버전 관리되므로, 특정 커밋 시점의 명세를 그대로 되짚을 수 있다. 참조 관계·상태·플래그·댓글 등 메타데이터는 노트북 서버의 DB에 둔다.

- [ ] 저장소를 등록하면 `docs/specs/` 아래 명세를 인식한다
- [ ] 저장소를 여러 개 등록할 수 있다 (프로젝트 = 저장소 1개)
- [ ] 프로젝트 초기화 시 `docs/specs/` 디렉터리 구조와 11단계 템플릿 껍데기가 커밋된다
- [ ] 프로젝트 초기화는 MCP 도구와 웹 양쪽에서 실행할 수 있다
- [ ] 플랫폼에서 저장하면 해당 저장소에 커밋된다
- [ ] GitHub에 직접 push된 변경이 플랫폼에 반영된다
- [ ] DB가 유실되어도 저장소만으로 참조 관계를 재구축할 수 있다
- [ ] 첨부 파일은 `docs/specs/assets/` 경로에 두고 MD에서 상대 경로로 참조한다

#### R6 단계별 상태

근거: [[SYNC-RFQ-001#Q3]]

문서 상태는 `초안` / `검토중` / `승인` 3단계. 상태는 게이트가 아니라 신호다. 확정 여부를 사람과 에이전트에게 알려줄 뿐, 하위 단계 작성을 막지 않는다. 작성·수정·승인 모두 권한 제한 없이 누구나 할 수 있다. 승인 여부는 팀원끼리 합의한 뒤 누른다.

- [ ] 문서 상태를 3단계 중 하나로 바꿀 수 있다
- [ ] 상태 변경 이력(누가·언제·무엇→무엇)이 남는다
- [ ] 상위 문서가 미승인이어도 하위 문서 작성·승인이 가능하다
- [ ] 승인 문서가 수정되면 `검토중`으로 내려간다
- [ ] 역할이나 권한에 따른 작성·승인 제한을 두지 않는다
- [ ] 규약 오류 또는 미완성 경고가 있는 문서는 `승인`으로 바꿀 수 없다
- [ ] `승인`으로 바꿀 때 상위 대조(R4)를 거친다

#### R7 댓글·리뷰

근거: [[SYNC-RFQ-001#Q3]]

문서 특정 줄에 스레드 댓글을 단다. 에이전트가 쓴 문서를 사람이 읽고 피드백하는 통로다.

- [ ] 줄 단위로 댓글이 붙고 스레드로 답글을 단다
- [ ] 이후 버전에서 줄이 이동해도 댓글이 따라간다 (못 찾으면 원본 위치 표시)
- [ ] 댓글을 "해결됨" 처리할 수 있고 미해결 수가 문서 상단에 표시된다

#### R8 프로젝트 대시보드

근거: [[SYNC-RFQ-001#Q3]]

프로젝트별로 11단계 진행 상태를 한 화면에서 본다.

- [ ] 프로젝트 목록에서 11단계 상태가 색으로 표시된다
- [ ] 프로젝트 상세에서 단계별 문서·플래그 수·미해결 댓글 수·최근 변경이 보인다
- [ ] 플래그나 오류가 있는 프로젝트는 목록에서 눈에 띈다
- [ ] 수치 클릭 시 해당 항목 목록으로 이동한다
- [ ] 11단계를 순서대로 이어서 읽는 모드가 있다

#### R9 MCP 서버

근거: [[SYNC-RFQ-001#Q2]]

에이전트가 툴에 상관없이 붙을 수 있도록 MCP 서버를 제공한다. **명세 본문을 쓰는 경로는 MCP와 GitHub 직접 push 둘뿐이다.** 사람은 자기 에이전트와 대화하다가 결정된 내용을 에이전트가 싱크독에 반영한다. 웹은 읽기·상태 변경·댓글·되돌리기까지만 한다.

- [ ] 특정 문서·항목을 조회하는 도구를 제공한다
- [ ] 문서·항목 조회 결과에 상태(초안/검토중/승인)와 버전이 함께 담긴다. 에이전트가 확정 명세와 초안을 구분할 수 있다
- [ ] 항목의 상위·하위 참조 목록을 조회하는 도구를 제공한다. 에이전트가 참조를 따라 필요한 것만 다시 요청할 수 있다
- [ ] 프로젝트의 문서 목록과 각 문서의 상태를 조회하는 도구를 제공한다
- [ ] 문서를 생성·수정하는 도구를 제공한다. 생성 시 문서 타입 템플릿이 적용된다
- [ ] 프로젝트를 초기화하는 도구를 제공한다
- [ ] 수정 도구는 조회 시 받은 버전을 함께 보내야 한다. 그 사이 문서가 바뀌었으면 쓰기를 거부하고 현재 버전과 내용을 돌려준다
- [ ] MCP로 쓴 내용도 사람이 쓴 것과 동일하게 버전 생성·참조 추출·변경 전파를 거친다
- [ ] MCP 작성분은 작성 주체가 에이전트로 기록된다
- [ ] Claude Code, Codex, Gemini 등 서로 다른 클라이언트에서 동작한다
- [ ] GitHub 저장소를 clone해 파일로 직접 읽는 것도 가능하다

#### R10 다이어그램 렌더링

근거: [[SYNC-RFQ-001#Q1]]

원본의 다이어그램 코드블록(mermaid)을 사람용 뷰에서 그림으로 렌더링한다. 렌더링은 브라우저가 하며 서버는 생성물을 만들지 않는다. 에이전트는 원본의 텍스트를 그대로 읽으므로 그림을 필요로 하지 않는다.

- [ ] mermaid 코드블록이 사람용 뷰에서 다이어그램으로 렌더링된다
- [ ] 원본이 바뀌면 그림이 다시 렌더링된다
- [ ] mermaid 문법 오류 시 원본 코드와 오류 메시지를 함께 보여주고 나머지 문서는 정상 렌더링된다
- [ ] 그림은 편집·내려받기 대상이 아니다. 고치려면 원본 MD의 코드블록을 고친다

### 3.2 비기능 요구사항

#### N1 성능
근거: [[SYNC-RFQ-001#Q4]]
- [ ] GitHub 변경이 1분 이내에 플랫폼에 반영된다
- [ ] 문서 1천 건, 참조 1만 건 규모에서 항목 참조 조회가 1초 이내다
- [ ] MCP 응답이 3초 이내다

#### N2 환경·접근
근거: [[SYNC-RFQ-001#Q4]]
- [ ] 노트북에서 서버를 띄우고 공개 주소(Cloudflare Tunnel)로 노출한다. 팀원은 위치에 상관없이 접속한다
- [ ] 사람은 GitHub OAuth로, 에이전트는 발급 토큰으로 식별된다
- [ ] 같이 일하는 사람만 들어온다. 저장소 접근 권한이 곧 접근 권한이다

#### N3 운영
근거: [[SYNC-RFQ-001#Q4]]
- [ ] GitHub 저장소가 단일 진실 원천이다. 명세·상태·버전·참조는 저장소만으로 재구축된다
- [ ] 플래그·전파결정·댓글은 원본에 없는 시스템 생성 정보이므로 DB에만 존재한다. 이 세 가지의 백업은 인프라 미결사항이다
- [ ] 문서는 삭제하지 않는다
- [ ] 플랫폼이 꺼져 있어도 팀원이 GitHub에서 문서를 읽고 쓸 수 있다

---

## 4. 성공지표

| 지표 | 목표 | 측정 |
|---|---|---|
| 싱크독 자체를 11단계 체인으로 완주 | RFQ~MINISPEC 전부 작성 후 코딩 시작 | 첫 프로젝트 |
| 명세 참조 보유율 | 하위 명세의 상위 참조 보유 100% | 매주 DB 쿼리 |
| 에이전트의 MCP 사용 | 팀원 전원이 각자 툴로 명세 조회에 성공 | 출시 후 첫 주 |
| 에이전트가 명세를 벗어나 구조를 정한 횟수 | 이전 방식 대비 감소 | 프로젝트 회고 |
| `확인 필요` 플래그 평균 처리 시간 | 2 영업일 이내 | 매주 DB 쿼리 |

---

## 5. ID 체계

```
문서 ID     {프로젝트코드}-{타입}-{번호}      예) SYNC-PRD-001
항목 ID     {문서ID}#{항목번호}              예) SYNC-PRD-001#R12
참조 문법   [[항목ID]] 또는 [[문서ID]]   — 참조 대상은 항목이 기본이며, 문서 전체를 근거로 삼을 때만 문서 ID를 쓴다
```

| 순서 | 타입 코드 | 단계 | 항목 ID 예시 |
|---|---|---|---|
| 1 | RFQ | RFQ | `#Q01` |
| 2 | PRD | PRD | `#G1`(목표), `#R12`(요구사항) |
| 3 | SCN | 사용자 시나리오 | `#P1`(페르소나), `#S03`(시나리오) |
| 4 | UC | USECASE | `#UC05` |
| 5 | INFRA | 인프라 아키텍처 | `#C03`(컴포넌트) |
| 6 | DOM | 도메인·클래스·데이터 | `#D01`(개념), `#C03`(클래스), `#T02`(테이블) — 도메인 모델, 클래스 명세, ERD/DD를 이 단계에 둔다. 문서는 여럿일 수 있다 |
| 7 | UI | 화면 명세 | `#V07`(화면) |
| 8 | API | API 명세 | `#POST/orders` |
| 9 | SEQ | SEQUENCE | `#S01` |
| 10 | MS | MINISPEC | 문서 단위 |
| 11 | CODE | CODE | 커밋·PR URL |
| — | STD | 표준 (단계 밖) | 규칙 항목. 명세 작성 규약·뷰 규약. 대시보드 11칸에 안 나오고 프로젝트 상세에 별도 묶음. 싱크독 프로젝트에만 있고 다른 프로젝트는 이걸 그대로 쓴다 |

규칙
- 프로젝트 코드는 영문 대문자 4자 이내
- 항목 번호는 문서 안에서 유일하며, 삭제해도 회수하지 않는다
- 참조는 하위 → 상위 방향으로만 적는다. 역방향(누가 나를 참조하는가)은 시스템이 계산한다
- 순서 변경 이력: 인프라·도메인 단계를 거치며 클래스 명세와 ERD를 도메인 단계(6)에 합치고, 화면 명세를 독립 단계(7)로 분리했다. ERD를 따로 두면 클래스와 같은 것을 두 번 그리게 되기 때문이다
- 되돌아오는 문서: 클래스 명세(DOM-002)는 6단계에서 엔티티(v1)까지 쓰고, API(8) 뒤에 컨트롤(v2)을 채우고, SEQUENCE(9) 뒤에 시퀀스가 드러낸 구멍을 반영(v3)한다. **SEQUENCE의 승인 조건은 그 v3 승인이다.** 단계 수는 11 그대로이며, DOM 단계가 v3까지 검토중으로 남는 것은 정확한 표시다

---

## 6. 미결사항

**착수 전 결정**
- [x] 기술 스택 → 인프라 아키텍처 단계에서 결정 — 결정: 인프라 3장에서 확정(FastAPI·PostgreSQL·React+Vite·MCP SDK·Cloudflare Tunnel)

**진행 중 결정**
- [x] MCP 쓰기 권한 범위 (전체 문서 vs 특정 프로젝트) — 결정: 묶지 않는다. 토큰은 발급자에게만 묶이고 그 사람이 보는 모든 프로젝트에 쓴다. 인프라 5장 "저장소 권한이 곧 접근 권한"과 같은 전제이고, 비목표에 역할·권한 관리가 있다. 나중에 좁혀야 하면 스키마 대신 저장 시점에 발급자의 저장소 권한을 확인한다
- [x] 알림 방식 → 별도 알림 없음. 내 할 일 화면이 플래그·전파 미결정·규약 오류·댓글을 직접 모아 보여준다. 이메일·메신저는 v2

